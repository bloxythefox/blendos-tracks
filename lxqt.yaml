impl: 'https://github.com/bloxythefox/blendos-tracks/raw/main'

track: 'blendos-desktop'

packages:
  - 'lxqt'
  - 'sddm'
  - 'xdg-desktop-portal-gtk' #No specific package available for LXQT, sating flatpak dep with generic package

services:
  - 'sddm'
